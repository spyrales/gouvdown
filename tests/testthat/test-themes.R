test_that(
  "theme_gouv() is a theme",
  expect_is(theme_gouv(), "theme")
)

test_that(
  "theme_grey_gouv() is a theme",
  expect_is(theme_grey_gouv(), "theme")
)

test_that(
  "theme_gouv_map() is a theme",
  expect_is(theme_gouv_map(), "theme")
)

